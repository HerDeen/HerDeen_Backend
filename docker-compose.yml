
services:
  app:
    container_name: herDeen-app
    build: .
    env_file:
      - .env
    ports:
      - "5000:8080"
    volumes:
      - ./src:/app/src
    depends_on:
      - mongo

  mongo:
    image: mongo
    restart: always
    ports:
      - "27018:27017"
 